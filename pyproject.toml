[project]
name = "linkcovery"
version = "0.1.0"
description = "Link discovery tool for people whit love and python :)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.10.5",
    "rich>=13.9.4",
]

[dependency-groups]
dev = [
    "ruff>=0.9.0",
]

[tool.ruff]
line-length = 120
